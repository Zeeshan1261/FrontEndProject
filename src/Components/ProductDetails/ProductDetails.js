import React from "react";
// import { useParams } from "react-router-dom";

function ProductDetails({fetch}) {
  
// const {id} = useParams();

return (  
<>
<h1>Testing the use Params </h1>
{/* {fetch.map((item)=> {
    return(
<>
<h1>{item.title}</h1>
<img alt="selectedProductImage" src={item.image}/>


</>
    )
})} */}

</>  
);
}

export default ProductDetails;
